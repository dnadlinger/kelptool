<% @page_title = 'Geräteübersicht' -%>

<%= render_biglist @categories, 'Gegenstände',
  :locals => {
    :display => {
      :children => lambda { |item_category| item_category == @category }
    }
  }
%>

<div class="navlinks">
  <p><%= link_to 'Kategorie hinzufügen', new_item_category_path %></p>
</div>