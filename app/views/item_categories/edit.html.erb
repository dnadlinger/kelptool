<% @page_title = @category.name + ' – Editieren' -%>

<%= show_errors @category, 'die Kategorie zu ändern' %>
<% edit_form_for @category do |f| %>
  <%= render :partial => f %>
  <p><%= f.submit 'Ändern' %> <%= link_to 'Zurück', :back, { :class => 'navlink' } %></p>
<% end %>